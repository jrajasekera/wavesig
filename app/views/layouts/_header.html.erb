<nav class="navbar navbar-expand-sm navbar-dark bg-dark">

  <% if user_signed_in? %>
  <a class="navbar-brand" href=<%= user_dashboard_path(current_user.id) %>><img src=<%= image_url('wavesiglogo.png') %> alt=""></a>
  <% else %>
  <a class="navbar-brand" href=<%= home_info_path %>><img src=<%= image_url('wavesiglogo.png') %> alt=""></a>
  <% end %>



  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample03" aria-controls="navbarsExample03" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarsExample03">
    <ul class="navbar-nav ml-auto">

      <% if not user_signed_in? %>
        <li class="nav-item">
          <a class="nav-link" href=<%= new_user_session_path %> >Login <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href=<%= new_user_registration_path %> >Sign Up <span class="sr-only">(current)</span></a>
        </li>
      <% else %>
        <li class="nav-item" data-toggle="tooltip" title="Dashboard">
          <a class="nav-link" href=<%= user_dashboard_path(current_user.id) %>><%= fa_icon "home", class: "fa-2x" %></a>
        </li>
        <li class="nav-item" data-toggle="tooltip" title="Friends">
          <a class="nav-link" href=<%= friends_path(current_user.id) %>><%= fa_icon "users", class: "fa-2x" %></a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" id="dropdown03" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= fa_icon "user", class: "fa-2x" %></a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown03">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'dropdown-item'  %>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href=<%= edit_user_registration_path %>>Edit Profile</a>
          </div>
        </li>
      </ul>
      <% end %>


  </div>
</nav>