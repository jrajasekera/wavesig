<nav class="navbar navbar-expand-sm navbar-dark bg-dark">

  <% if user_signed_in? %>
  <a class="navbar-brand" href=<%= user_dashboard_path(current_user.id) %>>
  <% else %>
  <a class="navbar-brand" href=<%= home_info_path %>>
  <% end %>

    <img src=<%= image_url('wavesiglogo.png') %> alt="">
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample03" aria-controls="navbarsExample03" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarsExample03">
    <ul class="navbar-nav ml-auto">

      <% if not user_signed_in? %>
        <li class="nav-item">
          <a class="nav-link" href=<%= new_user_session_path %> >Login <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href=<%= new_user_registration_path %> >Sign Up <span class="sr-only">(current)</span></a>
        </li>
      <% else %>
        <li class="nav-item">
          <a class="nav-link" href=<%= user_dashboard_path(current_user.id) %>>My Dashboard</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="http://example.com" id="dropdown03" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Account</a>
          <div class="dropdown-menu" aria-labelledby="dropdown03">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'dropdown-item'  %>
<!--            <a class="dropdown-item" href=<%#= destroy_user_session_path, method: :delete %>>Log out</a>-->
            <a class="dropdown-item" href=<%= edit_user_registration_path %>>Edit Profile</a>
          </div>
        </li>
        </ul>
      <% end %>


  </div>
</nav>